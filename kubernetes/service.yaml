apiVersion: v1
kind: Service

metadata:
  name: iris-service
  namespace: mlops

spec:

  selector:
    app: iris-api

  ports:

  - port: 80
    targetPort: 8080
    nodePort: 30080

  type: NodePort